terraform {
    backend "s3" {
        bucket         = "your-terraform-state-bucket"
        key            = "network/dev/network.tfstate"
        region         = "us-east-1"
        encrypt        = true
        dynamodb_table = "terraform-locks"
    }
}